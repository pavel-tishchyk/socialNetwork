export const isAuthSelector = state => state.authData.userData.isAuth;
export const loginSelector = state => state.authData.userData.login;
export const authIdSelector = state => state.authData.userData.id;
export const captchaUrlSelector = state => state.authData.captchaUrl;

